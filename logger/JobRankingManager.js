// @ts-check
"use strict";

import { MessageEmbed } from "discord.js"

export class 職業ランキングマネージャー {
  /*
  
    スレッド.messages.fetch()
    .then(メッセージリスト => console.log(`Received ${messages.size} messages`))
    .catch(console.error);
    */
  // 10まで
  constructor(メッセージ) {
  }

}